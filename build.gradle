// Requires gradle 7.2
// gradle clean build
// gradle clean test

allprojects {
    repositories {
        mavenCentral()
        mavenLocal()
    }
}

ext {
    releaseFileName = "${projectName}-v${version}.zip"
}
